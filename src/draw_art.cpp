#include <iostream>
#include "canvas.h"
using namespace std;
using namespace agl;

int main(int argc, char** argv)
{
   // Canvas drawer(800, 800);
   // drawer.stylizedCircle(PointAndColor(400, 400, Pixel{0, 63, 0}), 1000, 100);
   // drawer.maurerRose(6, 71,  PointAndColor(400, 400, Pixel{210, 185, 255}));
   // Pixel testPx; 
   // testPx.r = 210/255; 
   // testPx.g = 185/255; 
   // testPx.b = 255/255; 

   // drawer.stylizedCircle(PointAndColor(400, 400, Pixel{0, 63, 0}), 1000, 100);

   // Zoom
   // Canvas drawer(800, 800);
   // // why is there a repetition --> half circle still visible 
   // drawer.stylizedCircle(PointAndColor(400, 400, Pixel{1, 1, 1}), 1000, 300);
   // // drawer.subimage(600, 600, 200, 200);
   // drawer.save("zoom.png");

   // looks like a flower 
   // Canvas drawer(800, 800);
   // drawer.stylizedCircle(PointAndColor(400, 400, Pixel{1, 1, 1}), 40, 300);
   // drawer.save("test.png");


   // your code here
}

